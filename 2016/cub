#include <iostream>
using namespace std;

int main() {
	int n;
	cin >> n;
	int count = 0;
	for (int i = 1; i <= n; i++) {
		for (int j = i; j <= n; j++) {
			for (int k = j; k <= n; k++) {
				if (i * j * k == n) {
					count++;
				}
			}
		}
	}
	cout << count << endl;
	return 0;

}
